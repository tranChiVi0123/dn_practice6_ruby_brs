<%= provide(:title, t(:signup))%>
<%= provide(:type_btn, t(:signup))%>
<div class="row justify-content-center">
  <div class="col-6">
    <div class="login-form ">
    <h2 class="text-center"><%=t(:signup)%></h2>
    <%= render "shared/form"%>
  </div>
</div>